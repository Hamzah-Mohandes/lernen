import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            Text("Einstellungen")
                .font(.largeTitle)
                .padding()
            // Hier kannst du weitere Inhalte hinzufügen
            
        }
    }
}

#Preview {
    SettingsView()
} 
