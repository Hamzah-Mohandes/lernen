import SwiftUI

struct ProfileView: View {
    var body: some View {
        VStack {
            Text("Dies ist Ihr Profil.")
                .font(.largeTitle)
                .padding()
            // Hier kannst du weitere Inhalte hinzufügen
        }
    }
}

#Preview {
    ProfileView()
} 